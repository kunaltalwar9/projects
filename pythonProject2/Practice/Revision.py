'''
pytest - -html = file
name

driver.window_handles, driver.current.window_handle
import random,
import string

letters = string.ascii_lowercase
''.join(random.choice(letters) for i in range(1, 15))
random
email =

pytest.mark.skip, pytest.mark.


@pytest.fixtures
def beforTest:
    code
    yield
    code


def test_anyfunction(beforeTest)
    print(beforeTest)
    function will run before test_anyfunction)

    @pytest.mark.usefixtures('beforeTest')
    class TestExample:
        def test_some_function(self):
            pass

    @pytest.mark.usefixtures('dataLoad')
    class TestExample2:
        def test_editProfile(self, dataLoad):
            print(dataLoad)

    from selenium.webdriver.common.alert import Alert

    alert = driver.switch_to_alert
    alert.dismiss()

    with open('text.txt', 'r') as reader:

    class Something:
        def __init__(self):
            self.driver = webdriver.Firefox(executable_path=)

    action = ActionChains(driver)
    action.send_keys();
    action.move_to_element
    action.send_keys(Keys.CONTROL, '-');
    action.key_down(Keys.SHIFT)

    gst = Select(driver.find_element_by_xpath()
    gst.select
    by
    index, gst.select
    by
    visible
    text
    optns = gst.options()  # dropdown options

    driver.find_element_by_id("autosuggest").get_attribute("value")
    find_element_by_xpath("parent::div/parent::div/h4")
    assert a == b, print(a
    matches
    b)
    if driver.find_element_by_css_selector("span.discountPerc").text == '10%':
        driver.execute_script("window.scrollTo(0, document.body.scrollHeight);")
    driver.execute_script("window.scrollBy(0,-900)", "")
    from console directory
    name\ py.test: all
    files in the
    director

    # command line option through fixture command, for passing different values to test function

    def pytest_adoption(parser):
        parser.adoption("")

    @pytest.fixture(scope="class", params=[" chrome", " firefox", " IE"])

    '{}{}'.format(word, integer)
    import pdb;
    pdb.set_trace()
'''